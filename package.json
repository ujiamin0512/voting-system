{
  "name": "voting-system",
  "version": "1.0.0",
  "private": true,
  "description": "Voting system with admin panel, QR code sign-in and a live scoreboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "reset": "node -e \"require('fs').rmSync('data.json',{force:true});console.log('data.json cleared')\""
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "engines": { "node": ">=18" }
}
